repos:
  - repo: local
    hooks:
      - id: ruff-check
        name: ruff-check
        entry: uv run --active ruff check . --fix
        language: system
        pass_filenames: false

      - id: ruff-format
        name: ruff-format
        entry: uv run --active ruff format .
        language: system
        pass_filenames: false

      - id: pyright
        name: pyright
        entry: uvx pyright
        language: system
        pass_filenames: false
